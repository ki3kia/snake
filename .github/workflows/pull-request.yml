name: lint

on:
  pull_request:
    types: [opened, edited, reopened]

  workflow_dispatch:
  
jobs:
  pr_lint:
    runs-on: ubuntu-latest
    steps:
      - name: Check
        run: |
          npm run ci
          npm run lint
