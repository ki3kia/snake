name: lint

on:
  pull_request:
    branches: [main]

  workflow_dispatch:
  
jobs:
  pr_lint:
    runs-on: ubuntu-latest
    steps:
      - name: Check
        run: |
          npm run ci
          npm run lint
